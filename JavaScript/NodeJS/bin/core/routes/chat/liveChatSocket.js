import lvevents from './liveinvaderEvnet.js';

/**
 * 각 소켓의 컨트롤러 부분
 * @param {*} socket
 */
// const liveinvaderController = (socket) => {
//   socket.on(lvevents.connected, {});
//   socket.on(lvevents.disconnected, {});
//   //   socket.on(lvevents.);
// };

export default liveinvaderController;
