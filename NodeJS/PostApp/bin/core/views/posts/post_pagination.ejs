<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>페이지네이션</title>
  </head>
  <body>
    <h1>무슨 게시판</h1>
    <% if(total_post_count !== 0) { %>
       <table border="1">
      <colgroup>
        <col width="70" />
        <col width="250" />
        <col width="110" />
        
      </colgroup>
   
      <thead>
        <tr>
          <th>ID</th>
          <th>제목</th>
          <th>작성자</th>
          <th>생성일</th>
        </tr>
      </thead>
      <tbody> 
       <% data.forEach((lis, index) => { %>
         <tr>
          <td><%= lis.id %></td>
          <td>
          <%= lis.subject %>
          </td>
          <td><%= lis.name %></td>
          <td><%= lis.created_at %></td>
         </tr>
         <% }) %>
        <% } else { %>
          <div>
            <h2>등록된 글이 없습니다.</h2>
            <h1>새로 글을 입력해주세요.</h1>
          </div>
      </tbody>
      <% } %>
    </table border='1'>
  <div>
    <div>
  <ul> 
    <% for(let i = start_page; i <= end_page; i++) { %> 
      <% if(start_page < 1) { %>
        <a href="/boards/list/<%= i %>"><%= i %></a>
      <% } %>  
      <li value="<%= i %>" >
      <% if(i === curr_page) { %> 
       <a href="/boards/list/<%= i %>"><b><%= i %></b></a>
      <% } else { %>
       <a href="/boards/list/<%= i %>"><%= i %></a>
      <% } %>
    </li>
    <% } %>
  </ul>
  </div>
  </div>
  <div>
    <button onclick="location.href='/boards/write'">글쓰기</button>
    <form action="/boards/cleaner" method="post">
      <button type="submit">클리너</button>
    </form>
  </div>
  </body>
</html>
